[* this comment should be ignored completely *a *a *]
[* this comment as well ****************************]                     
[* aaaaand this one too ] ] ]]] *]
[*and*][*these*][*ones*][*too*]


[* operators/separators with spaces in between *]
= + - * / == != > < <= => ( ) { } ; # ,

[* operators/separators all together *]
=+-*/==!=><<==>(){};#,

[* operators/separators with new lines in between *]
=
+
-
*
/
=
!=
>
<
<=
=>
(
)
{
}
;
#
,

[*these*][*should*][*be*][*ignored*][*as*][*well*]